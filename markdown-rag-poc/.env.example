# Required API keys
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Optional: connect to ChromaDB server started with `docker compose up`
# Leave unset to use local PersistentClient (./chroma_db directory)
# CHROMA_HOST=localhost
# CHROMA_PORT=8000
